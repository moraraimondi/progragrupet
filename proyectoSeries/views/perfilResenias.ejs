<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nerfli</title>

    <link rel="stylesheet" href="/stylesheets/buttonHeader.css">
    <link rel="stylesheet" href="/stylesheets/perfiles.css">
    <link rel="stylesheet" href="/stylesheets/header.css">
    <link rel="stylesheet" href="/stylesheets/uikit.min.css">

    <script src="/javascripts/uikit-icons.min.js" ></script>
    <script src="/javascripts/uikit.min.js" ></script>
    <script src="/javascripts/home.js" ></script>
</head>
<body>
       <!--Include del header-->
  <%-include('./partials/header.ejs') %>
  <!--Include del header-->
<main> 
    <br>
    <br>
    <br>
    <br>
    <div class="papi">
        <div class="mini">
            <img src="https://cdn4.iconfinder.com/data/icons/small-n-flat/24/user-512.png" alt="" class="avatar">
        </div>
        <div class="hijos">
            <p>Usuario: <%=usuario.usuario %></p>
        </div>
        <div class="hijos">
            <p>Email: <%=usuario.email %></p>
        </div>
        <div class="hijos">
            <p>Cumpleaños: <%=usuario.birthday %></p>
        </div>
        <div class="hijos">
            <% if(usuario.genero_fav == null){%>
                <p>No tengo género favorito</p>
            <% } else {%>
            <a href="/series/porGenero?genreId=<%=usuario.genero_fav %>"><p>Género Favorito</p></a>
            <% } %>
        </div>
        <div class="hijos">
            <% if(usuario.serie_fav == null){%>
                <p>No tengo serie favorita</p>
            <% } else {%>
            <a href="/series/resultado?query=<%=usuario.serie_fav %>"><p>Serie Favorita</p></a>
            <% } %>
        </div>
    </div>
    <br>
    <br>
    <br>
    <br>
    <div class="resenias">
        <h2> Tus reseñas:</h2>
        <ul>
            <% for(i = 0; i < usuario.reviews.length; i++){ %>
                <% if(usuario.reviews[i].texto == null){%>
                    <p>No tengo reseñas</p>
                    <% } else {%>
                        <a href="/series/detalle?serieId=<%=usuario.reviews[i].idseries %>"><li><%=usuario.reviews[i].texto %></li></a>
                    <% } %>
           <% } %>
        </ul>
    </div>
</main>
</body>
</html>